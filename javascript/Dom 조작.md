```
# 순서
- 선택(Select) -> querySelector(selector) / querySelectorAll(selector)
- 변경(Maniplulation) -> createElement / SetAttribute 
	/ Element.append() / Node.appendChild / Node.innerText
	/ Element.innerHTML / ChildNode.remove() / Node.removeChild
	/ Element.setAttribute(name, value) / Element.getAttribute(attributeName)

# 이벤트
- EventTarget.addEventListener() -> 지정한 이벤트가 대상에 전달될 때 마다 호출할 함수 설정
- target.addEventListener(type, Listener)
- event preventDefault() - 현재 이벤트의 기본 동작 중단 -> 취소 불가능한 이벤트도 존재

```

