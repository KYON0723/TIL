```python
`ZeroDivisionError` : 0으로 나눌 때 발생

`NameError`  : 없는 변수명을 참조할 때 발생

` TypeError`  : 연산에 맞지 않은 Type일때 발생

`IndexError` :  잘못된 인덱스를 참조할 때 발생

`KeyError` : 없는 Key를 참조할 때 발생

`ModuleNotFoundError` : 없는 모듈을 사용하려 할 때 발생

`ImportError` : import할 파일이 없을 때 발생
```

